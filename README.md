# Java Bot #

* Install gradle (4.6)

##### Use in Intellij IDEA #####

* ``` gradle cleanIdea  ```
* ``` gradle Idea ```

##### Build the project #####
* ``` gradle build ```
* Check if it works: ``` java -jar build/libs/java-bot.jar ```